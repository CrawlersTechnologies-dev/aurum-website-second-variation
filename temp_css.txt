.setup-video-card {
  position: relative;
  border-radius: 20px;
  overflow: hidden;
  border: 1px solid var(--navy-line);
  background: linear-gradient(155deg, var(--navy-soft) 0%, var(--navy-deep) 100%);
  box-shadow: var(--shadow-lg);
  width: 100%;
  aspect-ratio: 16 / 9;
}

@media (max-width: 768px) {
  .setup-video-card {
    aspect-ratio: auto;
    height: 260px;
  }
}

.setup-video-card__frame {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  overflow: hidden;
}

.setup-video-card__player {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
  background: #000;
}
